"""
Package database — Base de données SQLite CTM.

Imports principaux :
  from app.database.connection import init_db, get_db
  from app.database.models import Ligne, Horaire, Tarif, Agence, Reclamation, Colis, CallLog
  from app.database import repository as repo
"""
